# Questions

Statuses: `DECIDED` (user answered) - `ASSUMED` (default taken, correct it if
wrong) - `FORK` (waiting on the user) - `DEFERRED` (not needed this milestone).

## Open forks

None. Both forks raised in round 1 were resolved the same round.

## Register

| ID | Question | Status | Answer or default | Landed |
|----|----------|--------|--------------------|--------|
| Q1 | Who is the primary user, and who else acts on the system? | DECIDED | The operator who runs cuttlefish is primary. External tools and other agents can submit tasks too, but they act inside whatever the operator has configured - the operator's trust boundary always wins. | PLAN §Users |
| Q2 | What is in the MVP? | DECIDED | One CLI trigger surface, a satay-workflow core loop from day one, episodic memory only, one delegation path to kopicode gated by a hardcoded allowlist, no multi-agent swarm beyond that one delegation. | PLAN §Scope |
| Q3 | What is explicitly out of the MVP? | DECIDED | Chat/webhook trigger surfaces, procedural and semantic memory, the general policy gate, real sandbox containment, any second delegation target, cuttlefish-crate as a separate product. | PLAN §Scope |
| Q4 | kopicode's ADR-0011 policy gate doesn't exist in code - how does slice 1 get it? | FORK -> DECIDED | Filed as kopicode board card KAN-987, for the agent already working in that repo to pick up. Not built inside this repo. | PLAN §Open risks, SLICES.md V1 |
| Q5 | Does slice 1 wire up real E2B containment around the delegation? | FORK -> DECIDED | No. Slice 1 runs against a throwaway scratch checkout with no adversarial-input assumption, the same accepted-risk posture kopicode's own ADR-0008 takes. Real containment is slice 2, gated on the identical trigger kopicode uses. | ADR-0002 |
| Q6 | How is a task addressed - its own ID, or something borrowed? | ASSUMED | A task's ID is the satay run ID that its workflow gets when started. No second identity scheme. | ADR-0001 |
| Q7 | Where does episodic memory actually live? | ASSUMED | Its own SQLite file (`.cuttlefish/episodic.db`), never inside satay's own `.satay/` store. satay owns its schema and its migrations; a second writer into that file would be an unversioned change to a schema this project doesn't own. | ADR-0004 |
| Q8 | Can two tasks run at once in the MVP? | ASSUMED | No. satay-runtime is one process, one writer, with no multi-worker execution yet (checked against its own CLAUDE.md), so sequential processing isn't a choice this project is making, it's a constraint it inherits. A second submission queues. | PLAN §Shape |
| Q9 | What does the CLI actually look like? | ASSUMED | `cuttlefish run "<task text>"` submits and blocks until the workflow reaches a terminal state, then prints a JSON result and exits with a code from a small fixed set. A daemon mode that accepts tasks without blocking is deferred. | PLAN §Affordances |
| Q10 | How does cuttlefish actually hand a task to kopicode? | DECIDED | `kopicode run --print` (kopicode's existing headless, newline-delimited-JSON surface) wrapped as a `side_effect=True` satay task. No new protocol. | ADR-0003 |
| Q11 | What answers cuttlefish's own LLM calls (not kopicode's)? | DECIDED | An `LlmProvider` seam, the same shape sibei-flow already uses: a keyless deterministic `replay` provider for tests, `claude` (a direct Anthropic credential) or `openrouter` (OpenRouter's OpenAI-compatible endpoint, the default — one key reaches many upstream models) for real use, selected by `CUTTLEFISH_LLM_PROVIDER`. | PLAN §Shape |
| Q12 | What does the sandbox provider abstraction look like, even though it isn't built until v2? | DECIDED | create / exec / snapshot / destroy, loosely matching the shape OpenAI's Agents SDK already standardised across seven sandbox providers, so this project is compatible with an emerging convention rather than inventing one. E2B is the only backend when it's built. | ADR-0002 |
| Q13 | Does the sandbox become its own product (cuttlefish-crate) now? | DECIDED | No. It stays an internal package inside this repo until a second real consumer exists or a genuinely differentiated capability (journal-aware sandbox snapshotting tied to a satay fork) is concrete enough to build. Spinning it out now repeats a mistake this suite already made once and reversed (kopicode's own ADR-0003). | ADR-0002 |
| Q14 | Which memory tiers are in the MVP? | DECIDED | Working memory (context budget and handover) and episodic memory (the durable log). Procedural memory (skill distillation) and semantic memory are both deferred past the MVP. | ADR-0004 |
| Q15 | How does a context handover actually get written? | DECIDED | Never a hand-maintained document. At a token-budget threshold, checkpoint the task via satay's own primitives, run one bounded LLM call over the recent journal window to distill a summary, discard the raw window, and keep a pointer back into the full journal for anything that needs it later. | ADR-0004 |
| Q16 | What happens when a kopicode delegation fails? | ASSUMED | Caught, journaled as a typed episodic event, and surfaced to the caller as a real failure. Not silently retried. | PLAN §Implementation decisions |
| Q17 | What happens if the kopicode binary isn't on PATH? | ASSUMED | A startup configuration error, checked before a task is even accepted, not a per-task failure discovered mid-run. | PLAN §Implementation decisions |
| Q18 | Does cuttlefish ask the operator a clarifying question when a task is ambiguous? | ASSUMED | Not in v1. The CLI is blocking and the operator is present, so this would be buildable, but it is genuinely extra scope past what the MVP needs to prove. Deferred. | PLAN §Scope |
| Q19 | Which satay-runtime version, and is the licence and offline story checked? | DECIDED | `satay` 0.1.0 on PyPI (confirmed live, 2026-08-23), Apache-2.0, runs fully offline over local SQLite. Pinned, not floated. | PLAN §Requirements |
| Q20 | What platforms and Python versions? | ASSUMED | Linux and macOS first-class, Windows best-effort, matching satay-runtime's own posture exactly, since this project can't be more portable than the runtime it sits on. Python 3.12 or 3.13. | PLAN §Requirements |
| Q21 | How would a person or a test know slice 1 actually works? | DECIDED | Four checkable claims: it runs a real task to a terminal state unattended, it survives a killed process and resumes without double-running the kopicode delegation, it delegates at least one real coding subtask to kopicode successfully, and it produces a journal a human can read afterward with no parallel hand-rolled transcript. | SLICES.md V1 |
| Q22 | Who holds the LLM and kopicode-related credentials, and where do they live? | ASSUMED | Environment variables only, never committed, never logged - the same posture as every sibling repo. cuttlefish's own episodic journal needs its own write-time redaction pass; satay's ADR-0029 redaction only covers satay's own journal schema; it does nothing for a second, cuttlefish-owned journal file. | ADR-0004 |
| Q23 | What is cuttlefish's actual trust model, given it's meant to hold credentials and run unattended? | ASSUMED | One trusted operator, same as kopicode's own ADR-0008, but the risk it accepts is different in kind: kopicode's worst case is a bad diff, reversible with `git revert`; cuttlefish's worst case is a long-lived credential misused with nobody watching. Named plainly rather than inherited by assumption. | ADR-0002 |
| Q24 | Are episodic events versioned from the first commit? | ASSUMED | Yes, the same discipline kopicode's own journal uses: an unmarshaller that preserves an unknown future event type rather than dropping it. | ADR-0004 |
| Q25 | kopicode board KAN-987 landed (2026-08-23, PR #109) with ADR-0011 decision 4 requiring orchestrator-side containment for any policy-gated invocation, naming cuttlefish's sandbox as the obligated party, no single-operator carve-out. Does slice 1 still use the policy gate for a real edit without the sandbox? | DECIDED | Yes. Slice 1 proceeds on ADR-0002's existing trust-model reasoning (one operator, their own task, their own checkout) - the same reasoning already accepted for the whole delegation, now extended explicitly to cover a policy-gated real edit rather than only the read-only/refusal case. Written down as a named exception, not silently resolved either way. | ADR-0002 addendum |
| Q26 | ADR-0002's own trigger for building the sandbox (multi-tenant exposure, or untrusted task input) hasn't fired, and `docs/SLICES.md` V2 names starting early as speculative work ahead of a real need. Does cuttlefish start V2 anyway? | DECIDED | Yes, but not by claiming the trigger fired - it hasn't. V2 starts to close a different, already-named gap: the compliance tension ADR-0002's first addendum recorded (kopicode ADR-0011 decision 4 requires containment for any policy-gated invocation, no single-operator carve-out) has been open and accruing real, unattended invocations since KAN-987 landed. The trigger condition stays as written for judging necessity; this is a separate, named reason for starting anyway. | ADR-0002 addendum, 2026-08-26 |
| Q27 | ADR-0002 originally said E2B is the only sandbox backend, but this project has no live E2B account yet, and kopicode ADR-0011 decision 4 requires "real process or **container** containment," not a microVM specifically. Does cuttlefish add a container-based backend too, and does that mean building Firecracker support directly instead of using E2B? | DECIDED | A container backend is added alongside E2B, implemented against the same create/exec/snapshot/destroy Protocol - not a new interface, and not the "second sandbox product" the original decision argued against, since it's one more minimal implementation of an interface this project already owns, running on infrastructure (a local container runtime) the operator's machine already has. Raw Firecracker was considered and rejected separately: E2B's own sandboxes already run on Firecracker, so it was never E2B-or-Firecracker at the isolation-mechanism level - only E2B's managed service versus this project operating Firecracker's jailer/image/networking stack itself, which repeats kopicode ADR-0008's "disproportionate effort" argument and drops Linux+KVM as a hard requirement, breaking macOS (Q20). | ADR-0002 addendum, 2026-08-26 |
| Q28 | What direction should cuttlefish take beyond slice 1/2's single delegated task? | DECIDED | Pivot into cuttlefish-crew: a multi-project fleet manager running teams of ~3 coding sub-agents per project, dashboard-observable, with automated context handover, steerable chat, and a hosting/runner story. | PLAN.md (pivot) §Problem/Solution |
| Q29 | Is the coding sub-agent backend still kopicode-only? | DECIDED | No. Pluggable from the start: kopicode remains the reference backend, a second backend (headless Claude Code) proves the abstraction isn't theoretical. Supersedes ADR-0003's "no new protocol, wrap kopicode as-is." | PLAN.md (pivot) §Shape, forthcoming ADR |
| Q30 | Does the rename (cuttlefish-agent -> cuttlefish-crew) change the Python package import path? | DECIDED | No. Repo/product/CLI branding become cuttlefish-crew externally; `import cuttlefish` and `pyproject.toml`'s `name = "cuttlefish"` are unchanged. | PLAN.md (pivot) §Shape |
| Q31 | Are each project's ~3 sub-agents functionally identical, just reskinned, or do they have distinct responsibilities? | DECIDED | Both: distinct functional roles (e.g. builder / reviewer / ops-and-demo-checker) and distinct personalities/voice, so dashboard output stays legible and lively rather than uniform. Full design deferred to the dashboard/team-design slice. | PLAN.md (pivot) §Users and actors |
| Q32 | Can a human redirect a sub-agent's work mid-task, not just read its history? | DECIDED | Yes - "steerable chat." Confirmed buildable on satay's existing `wait_for_event`/`send_event` durable pub/sub primitive (satay-runtime research, 2026-09-20); the workflow-shape work (racing a short wait against normal progress) is cuttlefish-crew's own, not a new satay primitive. Deferred to the handover/steering slice. | satay-runtime#98, satay-runtime#99, PLAN.md (pivot) §Open risks |
| Q33 | Is satay-runtime treated as a fixed external dependency going forward? | DECIDED | No. satay-runtime's own roadmap is now driven by cuttlefish-crew's needs; it is not to be treated as static. Filed as concrete asks: satay-runtime#98 (live query), satay-runtime#99 (steering docs/example), satay-runtime#100 (Postgres/multi-worker milestone). | PLAN.md (pivot) §Open risks |
| Q34 | Passing secrets (HuggingFace, GitHub, OpenRouter, per-project keys) to coding-agent backends is already a real pain (V2 hit it once, ad hoc, for one credential into one container). Should cuttlefish-crew build secrets management as a feature, and where does it sit in the roadmap? | DECIDED | Yes, as its own slice (slice B), positioned right after slice A rather than folded into it or deferred to the hosting slice - multiple projects each needing distinct, isolated credentials is a pain that starts as soon as slice A's multi-backend, multi-project shape exists, and both the dashboard (slice D) and runner/hosting (slice E) slices are better built on real scoped injection than ad hoc env vars. Start simple: an encrypted-at-rest secrets store scoped per project (optionally shared across projects), injected at sandbox-creation time through the same declared-policy mechanism already gating command/path access. A full credential-broker/proxy (agent never holds the raw key, only a scoped local proxy does) is the better long-term answer but explicitly deferred until the simple version's gaps are real, the same discipline ADR-0002 already uses against building ahead of a proven need. | PLAN.md (pivot) §Scope, §Open risks |
| Q35 | Claude Code's `Write`/`Edit` tool_use blocks report `file_path` as absolute, unlike kopicode's relative `edit_applied.path` (found running slice A live, 2026-09-20). Does `DelegationOutcome.edited_paths` normalise across backends, or does each backend leak its own path convention? | DECIDED | Normalises. `cuttlefish.delegate.claude_code.classify_stream` relativizes an edited path against `root` when given (verified against the real binary); a path genuinely outside `root` is kept absolute rather than raising. `cuttlefish.delegate.kopicode`'s own paths were already relative, so this makes both backends agree, not just kopicode's own convention winning by default. | ADR-0005, `cuttlefish.delegate.claude_code` |
| Q36 | Does `ClaudeCodeBackend` achieve real policy parity with kopicode's KAN-987 declared-allowlist gate? | DECIDED | Partially, named honestly rather than overclaimed. No declared `allow` commands maps to `--disallowedTools Bash` - verified live: the shell tool is absent from the model's toolset entirely, a real fail-closed result. A declared `allow` list maps to `--allowedTools Bash(<cmd>:*)` patterns, but that pattern's matching semantics are Claude Code's own, not kopicode's declared-allowlist grammar - unverified against every command shape kopicode accepts. See `cuttlefish.delegate.claude_code`'s module docstring and `docs/PLAN.md`'s Open risks. | ADR-0005 |
| Q37 | Does a sandboxed `ClaudeCodeBackend` delegation work for an operator authenticated through Claude Code's own OAuth login, not an API key? | DECIDED | No, and this is a named, accepted gap for this slice, not solved. Only `ANTHROPIC_API_KEY` is forwarded into a sandbox (mirroring kopicode's own credential forwarding) - this build's own `claude` binary authenticates via OAuth with no API key set at all (verified live), so a sandboxed delegation under that auth mode fails closed. Forwarding an OAuth session into a fresh sandbox was not attempted. | `cuttlefish.agents.claude_code` |
| Q38 | Slice B scopes secrets "per project," but there is no formal `Project` entity in the code yet (that's slice D's job). What identifies a project's scope in the meantime? | DECIDED | A plain string, `cuttlefish run --project NAME`, defaulting to `--root`'s own resolved directory name when omitted. `SecretsStore`'s own `scope` column is already just a string, so introducing a real `Project` entity later needs no store migration - only what supplies the string changes. | ADR-0006, PLAN.md Q38 |
| Q39 | A store-resolved secret never touches `os.environ` (ADR-0006: it's a local variable inside an already-side-effecting satay task). `cuttlefish.episodic.redact.Redactor` only ever looked up a fixed list of env-var names in `os.environ` - does a store-resolved secret get a blind spot in journal redaction? | DECIDED | Closed, not left open. `cuttlefish.cli`'s `_run()` resolves the same declared names against the same `SecretsStore` *before* constructing the `EpisodicStore`'s `Redactor`, passing a `lookup` that checks the resolved secrets first and `os.environ` second. Discovered live while implementing, not anticipated up front - named as its own decision because missing it would have been a real, silent leak path. | ADR-0006, `cuttlefish.cli._run` |
| Q40 | Should a declared `--secret NAME` that resolves to nothing in either scope fail at the CLI (before a task is accepted) or inside `delegate_to_agent_backend` (as a journaled `DelegationFailed`)? | DECIDED | The CLI, mirroring `_check_binary_on_path`'s existing "fail closed, before a task is even accepted" discipline (Q17) rather than the binary-missing-inside-a-sandbox case (which genuinely can't be checked until sandbox creation). `delegate_to_agent_backend` itself does no missing-name validation, trusting the CLI already did it - consistent with how the CLI never re-validates kopicode/claude's own on-PATH check inside the task either. | ADR-0006, `cuttlefish.cli._resolve_project_secrets` |
| Q41 | A sandboxed delegation always needed explicit credential forwarding (a fresh sandbox has no host environment); a direct, non-sandboxed delegation never did, because `asyncio.create_subprocess_exec`'s default `env=None` already inherits the whole parent `os.environ`, which covered kopicode/Claude Code's only two ambient credential names. Does a project-scoped secret need the non-sandboxed path touched at all? | DECIDED | Yes - a store-resolved secret with no ambient env-var counterpart (e.g. a HuggingFace token) would never reach a direct-host subprocess otherwise. `run_kopicode`/`run_claude_code` both gained an `env: Mapping[str, str] \| None` parameter, merged via `cuttlefish.delegate.subprocess_env.merge_env` as `{**os.environ, **env}` - `None`/empty still means exactly today's full inheritance, so a call declaring nothing is byte-for-byte unchanged. | ADR-0006, `cuttlefish.delegate.subprocess_env` |
| Q42 | Slice C's steering half needs satay's own control API reachable from outside the `cuttlefish run` process, which the runtime had no way to expose without pulling in `satay dev`'s whole interactive-session model. Does cuttlefish work around this inside its own codebase, or ask satay-runtime for the capability? | DECIDED | Asked satay-runtime directly, per the standing Q33 instruction. satay-runtime PR #101 added `satay.control.run_app` (ADR-0046 there): `run_app`'s exact ergonomics, the control/read API composed in for one block's lifetime, no new engine capability, no Postgres/multi-worker involved. Shipped as satay `0.2.0` (satay-runtime PR #102 bumped the version; rehearsed clean on TestPyPI; tagged `v0.2.0`; the real PyPI publish paused on the `pypi` GitHub environment's required-reviewer approval - Jian's own manual click, not something this session could or should do). **Update, 2026-09-21**: Jian approved the publish; satay `0.2.0` is confirmed live on PyPI (`https://pypi.org/simple/satay/`). cuttlefish's own pin is bumped to `satay==0.2.0` (`uv lock` re-resolved clean, `make ci` green). The steering half's own implementation is still ahead, pending its own design pass. | satay-runtime ADR-0046, PR #101/#102, ADR-0007 |
| Q43 | Should slice C build actual concurrent multi-agent execution (N roles literally running at once), or just make the handover mechanism per-agent-capable while still running one delegation at a time? | DECIDED | Real concurrency now. `cuttlefish.team.run_team` runs every role's delegation via `satay.gather` - verified live (2026-09-20) against a fake, deliberately-slow backend binary that two roles' work genuinely overlaps in wall-clock time, not just declared as concurrent. See ADR-0007. | ADR-0007, `cuttlefish.team` |
| Q44 | Running two roles concurrently against the real kopicode binary, pointed at the same `--root`, surfaced something neither cuttlefish nor satay caused: does it actually work, or does something serialize it? | DECIDED | The concurrency itself works (Q43); kopicode's own per-working-tree session lock (`.kopicode/lock`) does not. The second role's kopicode invocation starts concurrently, then immediately refuses ("another kopicode session is already running in this working tree") because the first already holds kopicode's own lock on that directory. Named as an accepted, real gap - not a bug to route around, since two agents editing one uncommitted working tree at once is a genuine hazard kopicode's lock is correctly guarding against. Two roles that need to genuinely edit concurrently need their own separate checkouts; unverified whether headless Claude Code enforces an analogous lock (no live run attempted, to avoid an unnecessary real API cost). | ADR-0007 |
| Q45 | ADR-0007's own kickoff sketch for the steering half was "race a short `wait_for_event` against normal delegation progress" via `satay.gather`. Does that composition actually work? | DECIDED | No - found unverified/unsupported before any code was written, not discovered as a bug later. `satay.wait_for_event`'s miss path raises `WorkflowParked`, a `BaseException` designed to unwind the *entire* workflow drive (handled only at the outermost per-run loop, per `satay/replay/engine.py`), not one `gather` member - `satay.gather`'s own docstring only documents task/`map`/`start_child` members, never `wait_for_event`, and nothing in satay-runtime's own test suite or cookbook mixes the two. Separately, neither backend's headless surface accepts input after it starts anyway (verified from source: a single argv positional, no stdin wiring) - inventing one would cross ADR-0003/0005's "no new protocol" boundary. Built instead: round-boundary redirection - after each delegation round's own outcome is journaled, one plain, sequential, non-gathered `wait_for_event(..., timeout=...)` decides whether to fold a queued message into one more round or finalize. Real, but bounded by however long the round in flight takes, not instant. | ADR-0008 |
| Q46 | `satay.control.run_app` needs FastAPI/uvicorn, gated behind satay's own `satay[studio]` extra (plain `satay` doesn't install them) - does `--steerable` become an opt-in extra of cuttlefish's own, or does cuttlefish's own pin just widen? | DECIDED | The pin widens: `satay==0.2.0` -> `satay[studio]==0.2.0`. `--steerable` is an ordinary, always-available CLI flag, not gated behind a second packaging layer cuttlefish would have to invent (`cuttlefish[steering]`) purely to defer a dependency satay already gates cleanly on its own side. | ADR-0008 |
| Q47 | Slice D needs a formal `Project` entity (Q38's own deferral). Where does its registry live, given every existing store (`secrets.db`, `episodic.db`, `.satay/`) is deliberately colocated with one project's own `--root`/cwd, and a fleet daemon oversees many projects from one process that isn't rooted in any of them? | DECIDED | A new store, `cuttlefish.projects.ProjectStore` at `~/.cuttlefish/projects.db` - hardcoded this slice, no `CUTTLEFISH_SECRETS_KEY`-style env override yet (real future work if a concrete need shows up, not built ahead of one). A `Project` row (`id`, `name`, `root`, `secrets_scope`, `roles`) is additive alongside every existing per-directory store, not a replacement - `secrets_scope` defaults to `name`, keeping Q38's plain-string convention readable by a project registered after the fact against an existing checkout. Role definitions on a `Project` store a persona (Q31's "personality/voice"), never task text - task text stays exactly as ephemeral as today's `--role NAME:TASK_TEXT`, composed fresh each time a team starts. | ADR-0009 |
| Q48 | Does the fleet daemon need satay-runtime's own multi-worker/Postgres milestone (satay-runtime#100, named in PLAN.md's Open risks as blocking "many projects, many teams, concurrently") to actually run several projects' teams at once? | DECIDED | No - verified directly against satay's source before designing around it, the same discipline ADR-0008 already used. `satay.control.run_app` takes a `data_dir` parameter (nothing assumes `Path.cwd()`), and `satay.timers`'s own poll-loop registry is keyed by `id(store)` (ADR-0030's own guard against **the same** store getting two poll loops, not different stores). N `satay.control.run_app(data_dir=...)` blocks, each pointed at a different project's own directory, run concurrently as plain `asyncio` tasks inside **one** Python process - no subprocess per project, no multi-worker capability needed. satay-runtime#100 remains exactly as unneeded as Q33/Q42 already found for one project's own team. | ADR-0009 |
| Q49 | `cuttlefish.runtime` is a plain module `global _runtime`, documented "call once, before starting any workflow" - does a daemon driving N projects' teams concurrently in one process need this changed? | DECIDED | Yes - the one real blocker Q48's finding surfaced. `_runtime` becomes a `contextvars.ContextVar[Runtime | None]` (`configure` -> `.set`, `current` -> `.get`); `asyncio.create_task()` copies the calling context, so a daemon task that calls `runtime.configure()` before starting its own `satay.control.run_app`/`satay.start` correctly scopes every task nested under it (verified against satay's own internal `asyncio.create_task` call sites) without leaking into a sibling project's task. A single `cuttlefish run`/`run-team` process calling `configure()` once at startup, exactly as today, is behaviourally unchanged - `ContextVar.set`/`.get` at the top of one linear flow acts exactly like the plain global it replaces. | ADR-0009, `cuttlefish.runtime` |
| Q50 | Slice D1's dashboard is scoped as a "fleet manager from day one" (Jian's own explicit choice over an observer-only design) - does `cuttlefish serve` shell out to `cuttlefish run-team --steerable` as a subprocess per project, or drive `run_task`/`run_team` in-process? | DECIDED | In-process, per Q48/Q49's findings - `cuttlefish.fleet.FleetDaemon` launches one `asyncio.create_task` per project's team (calling `run_team` directly, `satay.control.run_app(data_dir=<project root>/.satay)`), never a subprocess. Stopping a team uses satay's own already-built `POST /runs/{id}/cancel` (`ControlAPI.cancel`) against that project's own control API, not a signal to a process the daemon doesn't have. Steering reuses `cuttlefish.steering.send_steering_message` in-process, the identical function `cuttlefish steer` already calls. Status reads go straight to that project's own `.cuttlefish/episodic.db` (WAL-mode, already safe for a concurrent external reader), never satay's own live worker state, consistent with satay's own "reads are journal-derived, never live state" discipline. | ADR-0009 |

## Coverage

| Category | Covered by |
|----------|-----------|
| Primary user and actors | Q1 |
| Scope boundary | Q2, Q3, Q18 |
| Data model and identity | Q6, Q24, Q47 |
| State and storage | Q7, Q47 |
| Concurrency and conflict | Q8, Q43, Q44, Q48 |
| Interfaces and contracts | Q9, Q10, Q12, Q49, Q50 |
| Failure behaviour | Q16, Q17 |
| External dependencies | Q4, Q11, Q19 |
| Runtime and deployment | Q20, Q9 (Q49 resolves it) |
| Measurable success | Q21 |
| Security and secrets | Q22, Q23, Q25, Q26, Q27 |
| Versioning and migration | Q24 |
| Pivot direction (cuttlefish-crew) | Q28, Q29, Q30, Q31, Q32, Q33, Q34, Q35, Q36, Q37 |
| Secrets management (slice B) | Q34, Q38, Q39, Q40, Q41 |
| Team concurrency and steering (slice C) | Q32, Q33, Q42, Q43, Q44, Q45, Q46 |
| Dashboard and fleet daemon (slice D1) | Q31, Q38, Q47, Q48, Q49, Q50 |
